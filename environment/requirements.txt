# Motion Control Video Generation - Python Dependencies
# Install with: pip install -r requirements.txt

# Core ML frameworks (if not building from source)
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0

# Diffusion models and transformers
transformers>=4.38.0
diffusers>=0.28.0
accelerate>=0.27.0
safetensors>=0.4.0
peft>=0.8.0  # For LoRA training

# Video/Image processing
opencv-python>=4.8.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
av>=11.0.0
pillow>=9.0.0
scikit-image>=0.21.0

# Deep learning utilities
einops>=0.7.0
timm>=0.9.0

# Configuration management
omegaconf>=2.3.0
pyyaml>=6.0

# Video upscaling and restoration
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
realesrgan>=0.3.0

# Face detection and alignment
insightface>=0.7.3
onnxruntime>=1.16.0  # For insightface

# Frame interpolation (for extending video duration)
# Note: FILM/RIFE may need to be installed separately
# pip install git+https://github.com/google-research/frame-interpolation

# Optical flow (for temporal smoothing)
# Note: RAFT may need separate installation

# Progress and logging
tqdm>=4.66.0
tensorboard>=2.15.0
wandb>=0.16.0  # Optional: for experiment tracking

# Web interface
gradio>=4.0.0

# Utilities
requests>=2.31.0
huggingface-hub>=0.20.0
scipy>=1.11.0
numpy>=1.24.0
matplotlib>=3.7.0

# Development tools (optional)
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.1.0
# isort>=5.12.0
